#include <stdio.h>

int main() {
    int num;
    printf("Enter a number: ");
    scanf("%d", &num);

    // Use a for loop from 1 to 10
    for (int i = 1; i <= 10; i++) {
        ans = num * 1;
        printf("%d x 1 = %d", num, ans);
    }
    // For each iteration, print: num x i = result
    // Example: "5 x 1 = 5"

    return 0;
}